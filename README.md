# Detecção de Pneumonia via Raios-X

Este projeto foi desenvolvido como parte da disciplina de IA/Machine Learning no CIn-UFPE. O objetivo é classificar imagens de Raio-X em 'Normal' ou 'Pneumonia' utilizando uma rede neural ResNet18.

## 📁 Estrutura do Repositório
* `notebooks/`: Contém o código completo da solução.
* `submission.csv`: Arquivo com as predições geradas para a competição.
* `requirements.txt`: Lista de bibliotecas necessárias para rodar o projeto.

## 🛠️ Como rodar
1. Clone o repositório.
2. Instale as dependências: `pip install -r requirements.txt`.
3. Execute o notebook `Notebook_final.ipynb` no Kaggle ou Google Colab.

**Autor:** Ivan
